# name: Build Vue
# on: [push]
# permissions:
#   id-token: write
#   pages: write
# jobs:
#   build_vue:
#     runs-on: ubuntu-latest
#     name: Build Vue
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
#       - name: Vue App to Github Pages
#         uses: dencee/VuePagesAction@v1.0.0


name: Deploy Vue App to GitHub Pages

on:
  push:
    branches:
      - main  # or your default branch

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build Vue project
        run: npm run build

      - name: Upload dist folder
        uses: actions/upload-pages-artifact@v2
        with:
          path: dist  # assumes your built Vue files are in the dist/ folder

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
